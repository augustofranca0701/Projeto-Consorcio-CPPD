spring:
  datasource:
    url: jdbc:postgresql://${DB_HOST}:${DB_PORT}/${DB_NAME}
    username: ${DB_USER}
    password: ${DB_PASSWORD}

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  jwt:
    # lê direto do .env
    secret: ${JWT_SECRET:change-this-secret-please-change}
    expiration-ms: ${JWT_EXPIRATION_MS:86400000}
